import { CustomLineBreakerPipe } from './custom-line-breaker.pipe';

describe('CustomLineBreakerPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomLineBreakerPipe();
    expect(pipe).toBeTruthy();
  });
});
